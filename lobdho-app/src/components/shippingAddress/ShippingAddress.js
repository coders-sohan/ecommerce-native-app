import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const ShippingAddress = () => {
    return (
        <View>
            <Text>Shippping address</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default ShippingAddress;
